
/* 
    wrapper to be added into index js so that the editor works out of the box
    this is here for right now because I am testing html parsing before turning them all into components for a grid template type page

    // cover the wrapper element
    editor.on('load', () => editor.addComponents(`<div class="container" style="height:100%; background-color: white;"></div>`, { at: 0 }))
*/

/* 
    this code was in the blocks index
    Meant to be a form of checking that blocks are only added to the blocks maanger if they are in a master "blocks" array

    import {constants} from '../';

    const blockIds = constants.blockIds;
    const addBlock = (id, opts) => (Object.keys(blockIds).indexOf(id) >= 0) ? bm.add(id, opts) : null;
*/

/* 


*/